# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=robin_gateway
DB_USER=robin
DB_PASSWORD=changeme

# JWT Configuration
JWT_SECRET=change-this-to-a-very-long-and-secure-random-secret-key-minimum-64-characters

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Robin Backend URLs
ROBIN_CLIENT_API_URL=http://localhost:8090
ROBIN_SERVICE_API_URL=http://localhost:8080

# Application Configuration
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080
ROBIN_CONFIG_PATH=cfg/

# Logging
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_ROBIN=DEBUG
