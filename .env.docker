# Environment variables for Robin MTA Complete Suite
# Copy this file to .env and customize as needed

# Database Configuration (shared by Robin MTA and Robin Gateway)
DB_PASSWORD=robin123

# JWT Secret for Robin Gateway (CHANGE THIS IN PRODUCTION!)
# Generate a secure secret: openssl rand -base64 64
JWT_SECRET=change-this-to-a-very-long-and-secure-random-secret-key-in-production

# Optional: Override default ports if needed
# POSTGRES_PORT=5432
# REDIS_PORT=6379
# GATEWAY_PORT=8888
# UI_PORT=80
# SMTP_PORT=25
# SUBMISSION_PORT=587
# SMTPS_PORT=465
